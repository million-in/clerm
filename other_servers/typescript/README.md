# TypeScript Sample

Requires Node 24+.

```bash
node other_servers/typescript/server.ts
```

Environment:

- `CLERM_DAEMON_URL`: local daemon URL, default `http://127.0.0.1:8181`
- `PORT`: HTTP listen port
