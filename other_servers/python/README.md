# Python Sample

```bash
python3 other_servers/python/server.py
```

Environment:

- `CLERM_DAEMON_URL`: local daemon URL, default `http://127.0.0.1:8181`
- `LISTEN_ADDR`: listen host
- `PORT`: listen port
