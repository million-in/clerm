# Resolver Docs

- `resolver-architecture.md`: runtime design, schema loading, token verification, and dispatch flow
- `resolver-go.md`: embedding the resolver into an existing Go HTTP API
- `resolver-daemon.md`: running `clerm-resolver` as a local daemon for non-Go projects
- `other-languages.md`: TypeScript, Python, and Rust examples that use the daemon API
- `resolver-testing.md`: build, test, benchmark, and local verification commands
